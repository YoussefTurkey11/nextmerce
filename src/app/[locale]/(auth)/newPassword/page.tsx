import AuthLayout from "../layout";
import NewPasswordPage from "./NewPassword";

const page = () => {
  return (
    <AuthLayout>
      <NewPasswordPage />
    </AuthLayout>
  );
};

export default page;
