import AuthLayout from "../layout";
import ForgotPassword from "./ForgotPassword";

const page = () => {
  return (
    <AuthLayout>
      <ForgotPassword />
    </AuthLayout>
  );
};

export default page;
